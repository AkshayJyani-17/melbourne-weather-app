# Melbourne Weather App

A full-stack weather application for Melbourne built with React + Vite on the frontend and Express + TypeScript on the backend.

## Run locally

### Backend

```bash
cd backend
npm install
npm run dev
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## Deployment

The frontend is configured to proxy `/api` requests to the backend during development.
